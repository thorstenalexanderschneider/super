apiVersion: v1
kind: Service
metadata:
  name: jobsite-frontend-service
  namespace: jobsite
spec:
  selector:
    app: jobsite-frontend
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
